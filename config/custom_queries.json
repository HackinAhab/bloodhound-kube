{
    "queries": [
        {
            "name": "All Object Nodes and Edges",
            "description": "Retrieves all nodes and edges in the graph",
            "query": "MATCH p=(s)-[]->(t)\n\nWHERE s.cluster = 'default'RETURN p\nLIMIT 1000"
        },
        {
            "name": "All Object Nodes",
            "description": "Retrieves all nodes in the graph",
            "query": "MATCH (s)\nWHERE s.cluster = 'default'\nRETURN s\nLIMIT 1000"
        },
        {
            "name": "All Paths From Secrets",
            "description": "Retrieves all paths starting from nodes labeled 'Secret",
            "query": "MATCH p=(s:Secret)-[]->(t)\nWHERE s.cluster = 'default'\nRETURN p\nLIMIT 1000"
        },
        {
            "name": "All Paths from Pods running as root",
            "description": "Retrieves all paths starting from nodes labeled 'Pod' that have the property 'runAsRoot' set to true.",
            "query": "MATCH p=(s:Pod)-[]->()\nWHERE s.cluster = 'default' AND s.runAsNonRoot = FALSE AND s.runAsUser = 0\nRETURN p\nLIMIT 1000"
        },
        {
            "name": "All Paths from Pods running as root, exclude ScheduledOn Edge",
            "description": "Retrieves all paths starting from nodes labeled 'Pod' that have the property 'runAsRoot' set to true.",
            "query": "MATCH p=(s:Pod)-[r]->(t)\nWHERE s.cluster = 'default' AND s.runAsNonRoot = FALSE AND s.runAsUser = 0 AND NOT type(r) = 'ScheduledOn'\nRETURN p\nLIMIT 1000"
        },
        {
            "name": "All Paths to Nodes with External IPs",
            "description": "Retrieves all paths from HTTPRoute nodes to Nodes.",
            "query": "MATCH p=(s:External)-[*1..3]->(t:K8s_Node)\nWHERE s.cluster = 'default'\nRETURN p\nLIMIT 1000"
        },
        {
            "name": "All Aggregate Nodes",
            "description": "Retrieves all paths from nodes that are aggregates of other nodes.",
            "query": "MATCH p=(s)-[]->(t)\nWHERE s.cluster = 'default'\nWHERE t.name STARTS WITH 'ALL'\nRETURN p\nLIMIT 1000"
        },
        {
            "name": "Paths to AllSecrets",
            "description": "Retrieves all paths to the AllSecrets aggregate node.",
            "query": "MATCH p=(s)-[]->(t:AllSecrets)\nWHERE s.cluster = 'default'\nRETURN p\nLIMIT 1000"
        },
        {
            "name": "Paths to AllPods",
            "description": "Retrieves all paths to the AllPods aggregate node.",
            "query": "MATCH p=(s)-[]->(t:AllPods)\nWHERE s.cluster = 'default'\nWHERE t.name STARTS WITH 'ALL'\nRETURN p\nLIMIT 1000"
        },
        {
            "name": "Pods Sharing Process Namespace",
            "description": "Retrieves all pods with shareProcessNamespace enabled.",
            "query": "MATCH (p:Pod)\nWHERE p.cluster = 'default' AND p.shareProcessNamespace = TRUE\nRETURN p\nLIMIT 1000"
        }
    ]
}
